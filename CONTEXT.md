# InfiniteDSH

DSH 上的叙事插件：把灵叙式文字冒险 / 文学创作的引擎挂进 DeepSeek Harness。它是插件，不是独立软件。

## Language

**插件**:
本仓库交付的可安装 DSH bundle（及文学 preset）。用户仍使用 DSH 的会话、工作区、斜杠命令与 Web UI。
_Avoid_: 独立软件, App, 桌面端, 灵叙移植

**故事**:
一个 DSH 会话。侧栏里的一项就是一本；切会话即切书。不另建书架实体，也不用工作区当书。
_Avoid_: 书卡, Story 表, 书架上的一本, 工作区当书

**规则书**:
按故事绑定的世界设定条目集，常驻或按关键词注入模型上下文。插件包里的是模板；开书时拷到该会话专属目录，之后只读这份副本。切会话不得沿用上一本的文件或全局 last-active。
_Avoid_: 世界书（可作别名，正式用规则书）, 全局激活书

**会话目录**:
该会话自己的落盘目录，用来放规则书、角色卡等全套文件。优先用 DSH JSONL 为每个会话预留的 artifact 目录；若当前持久化后端没有逐会话路径，则落在插件自己的 `infinite/<sessionId>/` 下。不是工作区根目录，也不是多会话共享的设定夹。
_Avoid_: 工作区根, 项目目录, 全局 lore 文件夹

**正文**:
模型应当写给读者看的叙述。开局指令、模板标签、对话推荐、系统护栏都不是正文。
_Avoid_: 回复, 聊天记录

**随机世界事件**:
从当前会话所绑规则书里，抽出一条尚未命中、非常驻的条目，作为本回合可选剧情刺激。不是第二条模型请求。
_Avoid_: 场景分析, 后台进展, 对话推荐

**会话**:
DSH 的 session：一条可重放的事件日志。在本插件里，它同时是故事的身份与书写现场。
_Avoid_: 聊天窗口, 卷（除非以后明确映射）

**工作区**:
DSH 的 workspace：用户打开的一个目录，以及归属于它的会话列表。对模型不可见，不能单独充当「模型看见的故事身份」。
_Avoid_: 项目（口语可说项目，正式用工作区）

**文学会话**:
挂上本插件文学 preset 的会话。模型侧不提供 bash、改文件、子代理。
_Avoid_: 编码会话, author 模式（未交付）

**开局**:
人类命令 `/new [题材]`：把当前会话收成一本故事，按题材拷入专属文件，写入绑定，并给出开篇。不是分步卡牌向导。
_Avoid_: 开局向导, 选题材页

**档案**:
长篇压缩后写入本会话目录的剧情要点（`archive.md` 及角色摘记）。同一会话继续写，不另开新卷会话。
_Avoid_: 续卷, 旧卷只读

**dsh-infinite**:
本仓库对外安装的 Host 组合包名。纯算法在 `infinite-core`，文学 preset 与模板在 `dsh-infinite-preset`。
_Avoid_: dsh-narra, dsh-airp, 灵叙
